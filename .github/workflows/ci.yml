name: Validate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staged-update:
    runs-on: ubuntu-latest
    steps:
      - run: echo "AFO v0.3 file-by-file update in progress; final CI workflow will be restored after the verified tree is complete."
